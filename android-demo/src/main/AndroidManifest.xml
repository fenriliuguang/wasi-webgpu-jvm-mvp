<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WasiWebGpuDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Surface-only host for CM Guest instrumented tests (no Demo autorun).
             Separate taskAffinity avoids vivo redirecting launches to the launcher MainActivity. -->
        <activity
            android:name=".CmSurfaceTestActivity"
            android:exported="true"
            android:taskAffinity="io.github.fenriliuguang.wasi.webgpu.demo.cm_surface"
            android:launchMode="singleInstance" />
    </application>
</manifest>
